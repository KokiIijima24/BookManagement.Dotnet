@page
@model BookManagement.Web.Pages.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Register</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Register.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Register.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Register.Age)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Register.Age)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Register?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
